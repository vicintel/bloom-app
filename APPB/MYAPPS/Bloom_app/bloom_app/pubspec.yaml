name: bloom_app
description: "Bloom - AI-Powered Menstrual & Wellness Companion"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.1
  go_router: ^13.0.0
  flutter_dotenv: ^5.0.2
  http: ^1.6.0
  local_auth: ^2.1.6
  flutter_secure_storage: ^8.0.0
  shared_preferences: ^2.1.1
  table_calendar: ^3.1.3
  intl: ^0.19.0
  flutter_svg: ^2.2.3
  flutter_local_notifications: ^20.0.0
  provider: ^6.1.5+1
  privacy_screen: ^0.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
